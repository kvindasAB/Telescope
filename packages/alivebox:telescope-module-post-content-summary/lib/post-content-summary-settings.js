var postContentPreview = {
  propertyName: 'anotherPropertyName',
  propertySchema: {
    type: Boolean,
    label: "Show post content preview on Post List",
    optional: true

  }
}
addToSettingsSchema.push(postContentPreview);